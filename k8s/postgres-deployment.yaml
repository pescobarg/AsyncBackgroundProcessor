apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: asyncbackgroundprocessor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: AsyncBackgroundProcessor
            - name: POSTGRES_USER
              value: GenericUser
            - name: POSTGRES_PASSWORD
              value: GenericUser
          ports:
            - containerPort: 5432